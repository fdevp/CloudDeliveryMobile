namespace CloudDeliveryMobile.Android.Components.Geolocation
{
    public enum MarkerType
    {
        CurrentPosition,
        Salepoint,
        Order,
        AddedOrder,
        InProgressOrder,
        PassedRoutePoint,
        PendingRoutePoint,
        ActiveRoutePoint
    }
}